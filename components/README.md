YantraMitra component architecture placeholder.

The current production UI is intentionally preserved as 20 standalone Stitch HTML screens. Future reusable UI modules should be organized by domain in these folders without breaking the existing static-page imports.
